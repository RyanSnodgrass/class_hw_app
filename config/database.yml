# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
#

development:
  adapter: postgresql
  encoding: utf8
  database: postgres_class_hw_app_development
  pool: 5

test:
  adapter: postgresql
  encoding: utf8
  reconnect: false
  database: postgres_class_hw_app_test
  pool: 5

production:
  adapter: postgresql
  encoding: utf8
  database: postgres_class_hw_app
  pool: 5
